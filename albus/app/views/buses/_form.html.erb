<%= form_for(bus) do |f| %>
  <% if bus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bus.errors.count, "error") %> prohibited this onibus from being saved:</h2>

      <ul>
      <% bus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :placa %>
    <%= f.text_field :plate %>
  </div>

  <div class="field">
    <%= f.label :modelo %>
    <%= f.text_field :model %>
  </div>

  <div class="field">
    <%= f.label :nAcentos %>
    <%= f.number_field :nSeats %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

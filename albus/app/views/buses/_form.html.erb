
<%= form_for(bus) do |f| %>
  <% if bus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bus.errors.count, "error") %> prohibited this bus from being saved:</h2>

      <ul>
      <% bus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class="form-group">
		<%= f.text_field :plate, class: 'form-control', placeholder: "Placa", required: true %>
	</div>
	<div class="form-group">
  	<%= f.text_field :model, class: 'form-control', placeholder: "Modelo", required: true %>
	</div>
	<div class="form-group">
		<%= f.number_field :nSeats, class: 'form-control', placeholder: "Número de Acentos", required: true %>
	</div>
	<div class="form-group">
		<%= f.submit class:'form-control btn btn-primary' %>
	</div>
<% end %>
